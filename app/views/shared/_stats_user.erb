<% @user ||= current_user %>
<div class="stats">
  <a href="<%= watchlist_user_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.followed_series.count %>
    </strong>
    on watchlist
  </a>
</div>